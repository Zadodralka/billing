[pytest]
asyncio_mode = auto
testpaths = tests
# БД для тестов передаётся через окружение:
#   TEST_DATABASE_URL=postgresql+asyncpg://user:pass@localhost/billing_test pytest
# Без неё тесты, которым нужна БД, будут пропущены (см. tests/conftest.py).
