# Telegram-бот + веб-кабинет для продажи VPN-подписок

Полноценный сервис продажи VPN-доступа: Telegram-бот и веб-кабинет с единой базой
пользователей, приём оплаты через ЮMoney, автоматическая выдача конфигов через
Remnawave, баланс/промокоды/рефералка, подарочные подписки и админ-панель для
управления всем этим без доступа к серверу.

> 💚 **Поддержать проект**: [ЮMoney кошелёк 4100118233876883](https://yoomoney.ru/to/4100118233876883)

## Стек

- **Backend**: Python 3.11, FastAPI, aiogram 3
- **БД**: PostgreSQL + SQLAlchemy (async, asyncpg)
- **Очереди/сессии бота**: Redis (FSM-хранилище aiogram)
- **Оплата**: ЮMoney (HTTP-уведомления + проверка подписи и суммы)
- **VPN-панель**: Remnawave API
- **Веб**: Jinja2, без фреймворков на фронте (ванильный JS)
- **Почта**: SMTP (magic-link вход, подарочные коды)
- **Авторизация в кабинете**: Telegram Login Widget или email по ссылке (без пароля)
- **Планировщик**: APScheduler — отключение истёкших подписок, чистка старых аккаунтов,
  возврат баланса за зависшие неоплаченные счета

## Функционал

### Telegram-бот (клиент)
- `/start`, `/help`, принятие правил, приветствие, меню команд в Telegram-клиенте
- Покупка подписки: выбор тарифа, трафик (фиксированный объём или безлимит)
- Оплата через ЮMoney прямо в чате
- «Мои подписки» — список, реальный расход трафика, продление и доступ к конфигу
  каждой подписки прямо из чата
- «Мои конфиги» — QR-код и ссылка для быстрого подключения VPN
- «Баланс и бонусы» — баланс и реферальная ссылка (в т.ч. диплинк `?start=ref_XXXX`)
  прямо в боте
- Промокоды и реферальная программа (бонус рефереру и приглашённому на баланс)
- Напоминание об истечении подписки за несколько дней, чтобы продлить без разрыва доступа
- Тикеты поддержки — переписка с администратором прямо в боте
- Глобальный обработчик ошибок — бот не «зависает» молча при сбое

### Telegram-бот (администратор)
- `/stats` — быстрая сводка: пользователи, активные подписки, выручка за день/месяц, тикеты
- `/find <email или username>` — поиск пользователя и его подписок без захода в веб-админку
- `/broadcast` — рассылка сообщения всем пользователям с подтверждением перед отправкой
- `/tickets` — обработка обращений поддержки прямо в чате
- Уведомление о каждой новой оплате (сумма, тариф, покупка/продление/подарок)

### Веб-кабинет (клиент)
- Вход по Telegram Login Widget или по email (magic-link, без пароля)
- Личный кабинет: активные/приостановленные подписки, прогресс срока действия, история платежей
- Покупка и продление тарифов: промокоды, оплата с баланса (полностью или частично),
  YooMoney для остатка
- 🎁 **Подарочные подписки** — купить тариф в подарок по email. Получателю приходит
  письмо с кодом активации; при переходе по ссылке он сразу авторизуется и активирует
  подписку на своём аккаунте (отдельного письма для входа не требуется). Срок подписки
  считается с момента активации, а не покупки
- Реферальная программа — своя ссылка, статистика приглашённых, бонусы на баланс
- Тикеты поддержки с историей переписки
- База знаний — инструкции по подключению в виде markdown-статей
- Мобильная адаптация: отдельная раскладка меню, увеличенные тач-таргеты, PWA-иконка
  («Добавить на экран Домой»)

### Админ-панель
- Дашборд со сводной статистикой
- **Пользователи** — поиск, бан/разбан, ручное начисление баланса, права администратора,
  привязка email, удаление аккаунта
- **Подписки** — полный список, ручное продление/отключение/удаление, привязка к
  Remnawave-аккаунту
- **Платежи** — история, ручная отмена незавершённых платежей с возвратом баланса
- **Тарифы** — редактирование названия/цены/срока/трафика прямо из интерфейса,
  включение/выключение тарифа, отметка «Популярный выбор»
- **Промокоды** — создание, ограничение по числу использований и сроку действия,
  статистика применений
- **Поддержка** — ответы на тикеты клиентов
- **Инструкции** — редактор markdown-статей с живым превью (санитизация через DOMPurify)
- **Remnawave** — состояние VPN-панели

### Безопасность
- Проверка подписи и точной суммы (`withdraw_amount`) в уведомлениях ЮMoney — без этого
  комиссия платёжной системы ломала бы сверку сумм
- Защита от повторной обработки одного платежа (row-level lock на время активации)
- Атомарные операции с балансом и лимитом использований промокода (без гонок при
  параллельных оплатах)
- CSRF-защита (проверка Origin/Referer + SameSite-cookie), Secure-cookie в проде
- Rate limit на запрос magic-link писем
- Бан пользователя блокирует доступ и в боте, и в кабинете
- Экранирование пользовательского ввода в HTML-сообщениях бота

## Структура проекта

```
vpn_shop_bot/
├── bot/
│   ├── main.py                  # запуск бота, глобальный обработчик ошибок
│   ├── handlers/
│   │   ├── start.py             # /start, правила
│   │   ├── subscriptions.py     # мои подписки/конфиги
│   │   ├── payments.py          # покупка, активация подписки/подарка
│   │   ├── support_user.py      # тикеты со стороны клиента
│   │   └── support_admin.py     # тикеты со стороны администратора
│   ├── keyboards/
│   └── middlewares/              # авторизация пользователя, БД-сессия
├── web/
│   ├── main.py                  # FastAPI-приложение, CSRF-мидлварь
│   ├── routers/
│   │   ├── auth.py              # вход (email/Telegram), require_user/require_admin
│   │   ├── dashboard.py         # кабинет клиента, тарифы
│   │   ├── gift.py              # покупка и погашение подарочных подписок
│   │   ├── payments.py          # вебхук ЮMoney, создание/отмена платежей
│   │   ├── referral.py          # реферальная программа
│   │   ├── support.py           # тикеты клиента
│   │   ├── docs.py              # база знаний (публичная часть)
│   │   └── admin*.py            # админ-панель (users/subscriptions/payments/
│   │                             #   plans/promo/support/docs/remnawave)
│   ├── templates/
│   │   ├── base.html            # общий layout (сайдбар, мобильное меню)
│   │   ├── _plan_card.html       # общая карточка тарифа (макрос)
│   │   ├── _plan_card_style.html
│   │   ├── dashboard.html, plans.html, gift.html, gift_redeem.html, ...
│   │   └── admin/                # шаблоны админ-панели
│   └── static/
├── core/
│   ├── config.py                 # настройки из .env (pydantic-settings)
│   ├── version.py                 # читает /VERSION, доступно в шаблонах как app_version
│   ├── database.py                # SQLAlchemy async engine/session
│   ├── models.py                  # ORM-модели (User, Subscription, Payment, GiftCode, ...)
│   ├── plans.py                   # тарифы (хранятся в БД, редактируются из админки)
│   ├── promo_referral.py          # промокоды, баланс, реферальные бонусы
│   ├── remnawave.py                # клиент Remnawave API
│   ├── yoomoney.py                 # клиент ЮMoney
│   └── email.py                    # отправка писем (magic-link, подарки)
├── scheduler.py                    # APScheduler: истёкшие подписки, зависшие платежи
├── migrations/
│   └── applied/                    # ручные SQL-миграции для обновления БД (см. ниже)
├── VERSION                          # версия системы (правится вручную)
├── .env.example
├── docker-compose.yml
├── Dockerfile
└── requirements.txt
```

## Установка

### Требования

- Чистый сервер на Ubuntu/Debian (для автоустановки Docker скриптом ниже; на других
  дистрибутивах поставьте Docker + Compose plugin вручную и переходите к шагу 2)
- Telegram-бот — токен из [@BotFather](https://t.me/BotFather)
- Панель [Remnawave](https://remna.st/) с API-токеном
- Кошелёк ЮMoney с включёнными HTTP-уведомлениями об оплате
- SMTP-аккаунт для отправки писем (вход по email, подарочные коды)

### Быстрый СТАРТ — одним скриптом

```bash
git clone https://github.com/Zadodralka/billing.git vpn_shop_bot
cd vpn_shop_bot
sudo ./scripts/install.sh
```

Скрипт сам: ставит Docker, если его ещё нет; создаёт `.env` из `.env.example` и
генерирует `SECRET_KEY`/`POSTGRES_PASSWORD` (их можно не придумывать самому); если
бизнес-секретов (токен бота, Remnawave, ЮMoney, SMTP) в `.env` не хватает —
останавливается и говорит, что дозаполнить — просто откройте `.env`, впишите их
и запустите `sudo ./scripts/install.sh` ещё раз, он продолжит с того же места и
не тронет уже заполненные значения. В конце по желанию настроит nginx + HTTPS
через certbot (спросит домен). Безопасно перезапускать в любой момент.

После — не забудьте прописать вебхук ЮMoney на
`https://ВАШ_ДОМЕН/payment/webhook/yoomoney` в настройках кошелька.

### Вручную, по шагам

Тот же результат, если нужен контроль над каждым шагом (или сервер не на Debian/Ubuntu):

1. Склонируйте репозиторий и перейдите в него:
   ```bash
   git clone https://github.com/Zadodralka/billing.git
   cd billing
   ```

2. Создайте `.env` из примера и заполните переменные (таблица ниже):
   ```bash
   cp .env.example .env
   ```

3. Сгенерируйте `SECRET_KEY` (не короче 32 символов, иначе приложение откажется стартовать):
   ```bash
   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
   ```

4. Задайте `POSTGRES_PASSWORD` в `.env` — тот же пароль должен быть частью `DATABASE_URL`
   (`postgresql+asyncpg://vpnbot:ПАРОЛЬ@db:5432/vpnbot`).

5. Запустите:
   ```bash
   docker compose up -d --build
   ```
   При первом старте таблицы в БД создаются автоматически (SQLAlchemy `create_all`) —
   вручную ничего накатывать не нужно.

6. Настройте вебхук ЮMoney на `https://ВАШ_ДОМЕН/payment/webhook/yoomoney`
   в настройках уведомлений кошелька.

7. nginx + TLS ставятся отдельно от docker-compose (на хосте, не в контейнере) -
   см. `scripts/install.sh` для готового конфига или настройте вручную + `certbot --nginx`.

### Обновление действующей установки

При `git pull` нового кода **пересоберите и перезапустите** контейнеры — `web`/`bot`
запущены без hot-reload, простого `git pull` недостаточно:
```bash
git pull
docker compose up -d --build
```

Если в обновлении появились новые файлы в `migrations/applied/*.sql` — накатите их
на боевую БД вручную (это не Alembic-миграции, а обычные SQL-скрипты с
`IF NOT EXISTS`/`IF EXISTS`, безопасно перезапускать):
```bash
psql "$DATABASE_URL" -f migrations/applied/имя_миграции.sql
```

### Переменные окружения

| Переменная | Обязательна | Описание |
|---|---|---|
| `BOT_TOKEN` | да | Токен Telegram-бота |
| `ADMIN_IDS` | да | Telegram ID администраторов через запятую |
| `POSTGRES_PASSWORD` | да | Пароль Postgres в docker-compose — должен совпадать с паролем в `DATABASE_URL` |
| `DATABASE_URL` | да | PostgreSQL DSN, напр. `postgresql+asyncpg://vpnbot:пароль@db:5432/vpnbot` |
| `REDIS_URL` | да | Адрес Redis, напр. `redis://redis:6379/0` |
| `SECRET_KEY` | да | Секрет для подписи веб-сессий (≥32 символов) |
| `SESSION_HTTPS_ONLY` | нет (default `True`) | `False` только для локальной разработки без HTTPS |
| `SESSION_MAX_AGE` | нет | Время жизни веб-сессии в секундах |
| `YOOMONEY_RECEIVER` | да | Номер кошелька ЮMoney |
| `YOOMONEY_SECRET` | да | Секрет HTTP-уведомлений ЮMoney |
| `REMNAWAVE_URL` | да | URL панели Remnawave |
| `REMNAWAVE_TOKEN` | да | API-токен Remnawave |
| `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM` | да | Данные SMTP для отправки писем |
| `WEBAPP_URL` | да | Публичный URL веб-кабинета (используется в ссылках писем, вебхуке) |
| `PLAN_1M_PRICE`, `PLAN_3M_PRICE`, `PLAN_6M_PRICE`, `PLAN_1Y_PRICE` | нет | Стартовые цены тарифов — далее редактируются из админки |
| `PLAN_*_UNLIMITED_EXTRA` | нет | Доплата за безлимитный трафик к каждому тарифу |
| `REFERRAL_BONUS_REFERRER` / `REFERRAL_BONUS_REFERRED` | нет | Реферальные бонусы в рублях (по умолчанию 100/50) |
| `TIMEZONE` | нет (default `UTC`) | Часовой пояс для отображения дат/времени, имя из базы IANA (`Europe/Moscow`, `Asia/Yekaterinburg`) — в БД всё хранится в UTC, меняется только вывод |
| `ADMIN_GROUP_CHAT_ID`, `ADMIN_TOPIC_*` | нет | Уведомления админам в группу с топиками вместо личных сообщений — см. ниже |

### Уведомления в группу с топиками

По умолчанию все уведомления админам (новая оплата, сообщение в поддержку,
сбой планировщика, бэкап) приходят личным сообщением от бота каждому ID из
`ADMIN_IDS`. Если админов несколько или просто хочется разложить уведомления
по темам вместо одной ленты в личке — можно направить их в Telegram-группу с
топиками (форум-группа).

1. Создайте supergroup в Telegram, в настройках группы включите **Темы**
   (Topics/Forum).
2. Добавьте бота в группу и выдайте ему права администратора (нужно право
   отправлять сообщения и — если хотите, чтобы бот сам создавал топики —
   управлять темами; создать темы можно и вручную, этого достаточно).
3. Создайте темы под нужные категории, например: 💳 Платежи, 🎫 Поддержка,
   ⚙️ Система, 🗄 Бэкапы. Можно завести не все четыре — то, что не завели,
   будет приходить в общий чат группы без темы.
4. Узнайте ID группы и ID каждой темы — проще всего через ссылку на тему:
   откройте нужную тему в Telegram Desktop/Web → «Скопировать ссылку». Она
   выглядит как `https://t.me/c/1234567890/45`, где:
   - `1234567890` — это ID группы без записи `-100` в начале, то есть
     `ADMIN_GROUP_CHAT_ID=-1001234567890`;
   - `45` в конце — ID темы, например `ADMIN_TOPIC_PAYMENTS=45`.
   Общая тема группы (General) отдельного ID не имеет — сообщения без темы
   просто не указывают `message_thread_id`.
5. Впишите значения в `.env` и перезапустите: `docker compose up -d --build
   web bot scheduler` (уведомления шлются из всех трёх сервисов).

Для бэкапов (`scripts/backup.sh`) поддержка группы работает так же — если
`ADMIN_GROUP_CHAT_ID` заполнен, архив уходит одним сообщением в группу
(в тему `ADMIN_TOPIC_BACKUPS`, если она указана), а не в личку каждому админу.

### Версия системы

Файл `VERSION` в корне репозитория — правьте вручную при значимых изменениях,
чтобы не путаться, какой билд сейчас в проде. Видна в подвале сайдбара
администраторам в веб-кабинете.

## Тесты

Тесты гоняются на настоящем Postgres (не sqlite), нужна отдельная пустая БД.
`pytest` не входит в боевые зависимости — ставится отдельно:

```bash
pip install -r requirements-dev.txt
createdb billing_test   # или CREATE DATABASE billing_test; через psql
TEST_DATABASE_URL=postgresql+asyncpg://user:pass@localhost/billing_test pytest
```

Без переменной `TEST_DATABASE_URL` тесты, которым нужна БД, будут пропущены
(а не упадут). Схема создаётся автоматически, каждая проверка стартует с
чистых таблиц. Внешние API (Remnawave, Telegram) в тестах замоканы.

## Бэкап и восстановление на другом сервере

Скрипты в `scripts/` собирают всё, что не лежит в git и нужно для быстрого
переезда на новый сервер: дамп БД, `.env` с секретами, загруженные файлы
(`web/static/uploads` — картинки в статьях документации), конфиг nginx.
Код и SQL-миграции уже под git, они приходят через `git clone`.

### Бэкап

```bash
./scripts/backup.sh
```

Кладёт архив `backups/backup_ГГГГММДД_ЧЧММСС.tar.gz` (права `600` — внутри
секреты) и сам подчищает локальные бэкапы старше 14 дней. Настраивается
через переменные окружения:

| Переменная | По умолчанию | Назначение |
|---|---|---|
| `BACKUP_DIR` | `./backups` | Куда класть архивы |
| `BACKUP_KEEP_DAYS` | `14` | Через сколько дней удалять старые локальные архивы |
| `NGINX_CONF` | `/etc/nginx/sites-available/vpnbot` | Откуда брать конфиг nginx |
| `REMOTE_BACKUP_PATH` | — | Если задано — архив дополнительно копируется туда (`rclone`, а если его нет — `scp`) |
| `TELEGRAM_BACKUP_NOTIFY` | — | Если `1` — копия бэкапа (БД + загруженные файлы, **без `.env`**) уходит всем `ADMIN_IDS` из `.env` документом в Telegram через того же бота. Секреты по Telegram намеренно не гоняются - см. комментарий в скрипте |

**Архив с `.env` внутри нельзя хранить только на этом же сервере** — если он
упадёт вместе с диском, бэкап пропадёт вместе с системой. Настройте
`REMOTE_BACKUP_PATH` на другой сервер/облако, или скачивайте `backups/`
регулярно вручную. Telegram-уведомление (`TELEGRAM_BACKUP_NOTIFY=1`) не
заменяет это — оно без `.env`, только для быстрого восстановления данных.

Для регулярного запуска — cron на сервере (не настраивается автоматически).
Например, каждый день в полночь с отправкой копии в Telegram:
```cron
0 0 * * * cd /путь/к/billing && TELEGRAM_BACKUP_NOTIFY=1 ./scripts/backup.sh >> /var/log/vpnbot-backup.log 2>&1
```

### Восстановление на новом сервере

```bash
git clone https://github.com/Zadodralka/billing.git
cd billing
./scripts/restore.sh /путь/к/backup_ГГГГММДД_ЧЧММСС.tar.gz
```

Скрипт поднимает только контейнер БД, накатывает в него дамп, возвращает
`.env` и загруженные файлы на место (существующий `.env`, если найден, не
затирается молча — сохраняется рядом как `.env.bak.ВРЕМЯ`). Конфиг nginx
копируется в `./nginx_vpnbot.conf.restored` — установить его в систему и
выпустить TLS-сертификат (`certbot --nginx -d ВАШ_ДОМЕН`) нужно вручную,
это осознанно не автоматизировано. Дальше — обычный `docker compose up -d
--build`, как при первой установке.

### Восстановление подписок без бэкапа (по данным Remnawave)

Если бэкапа биллинга не было, а сервер Remnawave — отдельная машина —
пережил сбой, действующие VPN-аккаунты там остались, просто биллинг
о них больше не знает (пустая БД после переустановки). Такие аккаунты
продолжают работать, но у пользователей пропадает подписка из личного
кабинета/бота и не приходят уведомления. `scripts/restore_from_remnawave.py`
разбирает список аккаунтов из Remnawave, находит/создаёт пользователей
по telegram_id и email и восстанавливает записи подписок в биллинге,
подключая их к уже существующим (не новым) аккаунтам в Remnawave:

```bash
# Сначала посмотреть, какие поля реально отдаёт ваша панель
docker compose exec web python3 scripts/restore_from_remnawave.py --dump 3

# Сухой прогон - ничего не пишет, только показывает план действий
docker compose exec web python3 scripts/restore_from_remnawave.py

# Применить
docker compose exec web python3 scripts/restore_from_remnawave.py --apply
```

Повторный запуск безопасен — уже восстановленные подписки не дублируются,
только обновляются.

## Лицензия

Проект распространяется под лицензией [MIT](LICENSE) — используйте, изменяйте
и разворачивайте свободно, в том числе в коммерческих целях. Единственное
условие — сохранять уведомление об авторстве из файла LICENSE.
